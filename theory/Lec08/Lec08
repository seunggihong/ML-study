2020/08/06

Lec 8

# Logistic Regression ( 로지스틱 회귀 )
    Classification ( 분류 )

    Logistic vs Linear
    - Logistic  : 구분 가능한 데이터
    - Linear    : 연속적인 데이터 

    Hypothesis
        g(Z) function out value is between 0 and 1
        - Sigmoid ( Logistic ) function
        :: g(Z) = e^z / ( e^z + 1 ) = 1 / ( 1 + e^-z )

        Decision Boundary ( 결정 경계 )

    in X -> Wx + b -> Sigmoid -> 0.5 -> out 0 or 1

    Cost function
        A convex logistic regression cost function
        - cost( h(x), y ) = -ylog( h(x) ) - ( 1 - y )log( 1 - h(x) )

        Optimization



## Main point keyword ##

Logistic Regression / Classification
Sigmoid